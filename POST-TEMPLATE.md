# 文章范式

写新文章时照这份抄。**这个文件本身不是文章** —— 它放在仓库根目录，不会被构建到 `dist/`，也不会出现在站点上（只有 `public/` 下的文件会进产物）。

文章源文件放在：

```
src/content/posts/
```

文件名决定 URL：`redis-as-queue.md` → `/posts/redis-as-queue/`

建议用**小写英文 + 连字符**。中文文件名能用，但 URL 会被编码成一长串百分号，不好看也不好分享。

---

## 一、可复制的模板

下面这一块整段复制，改内容即可（最外层是四道反引号，所以里面的代码块可以照常用三道的）：

````markdown
---
title: 文章标题
date: 2026-09-14
summary: 六十到一百字左右的摘要，列表页和搜索引擎都用它。写清楚这篇解决了什么问题、结论是什么，不要写成"本文介绍了……"。
tags:
  - 标签一
  - 标签二
series: 系列名
order: 1
---

## 第一节

正文。

```rust
// 代码块标上语言，才会有语法高亮
fn main() {}
```

> 引文或旁注。这一段会渲染成楷体，并且比周围的面板更透明，
> 用来区分"别人的话"和"我写的话"。

## 第二节

正文。
````

不需要的都删掉 —— `series` / `order` / `updated` / `draft` 都是可选的。

---

## 二、字段说明

| 字段 | 必填 | 类型 | 约束与说明 |
| --- | --- | --- | --- |
| `title` | ✅ | 字符串 | 不能为空。文章页的 `<h1>` 由它生成 |
| `date` | ✅ | 日期 | 用 `YYYY-MM-DD`。**排序依据**，也是归档分组的依据 |
| `summary` | ✅ | 字符串 | **最多 200 字**。列表页、`<meta description>`、RSS 都用它 |
| `tags` | | 字符串数组 | 不写则为空数组。会自动生成标签页 |
| `series` | | 字符串 | 系列名。写了才会出现在「系列」导航下 |
| `order` | | 正整数 | 系列内的阅读顺序。**不能是 0 或负数** |
| `updated` | | 日期 | 填了会在文章页显示「更新于 …」 |
| `draft` | | 布尔 | `true` 时构建会跳过这篇，不会出现在任何页面 |
| `lang` | | 字符串 | 默认继承站点语言，一般不用写 |

**没有 `category` 字段。** 本站只有标签，没有分类体系 —— 这是刻意的：分类容易变成需要维护的层级，标签不用。

### 关于 `summary` 的 200 字上限

这是硬限制，超了会**构建失败**并告诉你超了多少。实际写 60~110 字最合适 —— 再长列表页的节奏就散了。

### 关于 `series` 和 `order`

系列是本站唯一使用编号的地方，因为系列**真的是序列**，编号有信息含义（第 1 篇该先读）。

```yaml
series: 性能手记
order: 1
```

- 同一系列内按 `order` 升序排；没写 `order` 的排在最后，退回按日期升序
- `order` 只影响系列页和「第 N / M 篇」的标注，**不影响归档和文章列表的顺序**（那两处只看 `date`）
- 所以你可以先写第 3 篇、过几天再补第 1 篇，顺序不会乱

---

## 三、正文写法

### 标题层级

**从 `##` 开始，不要用 `#`。** 文章标题由 frontmatter 的 `title` 提供，正文里再写一个 `#` 会出现两个一级标题。

`##` 和 `###` 会被自动收进右侧目录。`####` 及更深的不会。

### 代码块

标语言才有高亮：

````markdown
```rust
let x = 1;
```
````

代码块是全站唯一比周围**更透明**的元素（其他面板都是越靠上越厚），所以它在视觉上是"透过玻璃看到的底层内容"。长文里代码块多也不怕，模糊半径是按"背后有没有东西"给的，成本可控。

### 引文与旁注

用 `>`，渲染出来是楷体 + 更薄的面板：

```markdown
> 队列的可靠性不在 push 那一步，在 ack 那一步。
```

适合放：操作手册里的一句话、同事的原话、自己的旧笔记。**不适合**放名言警句。

### 表格

标准 Markdown 表格，宽度不够时会横向滚动：

```markdown
| 方案 | 重启是否丢数据 | 复杂度 |
| --- | --- | --- |
| BRPOP | 是 | 低 |
| Stream | 不丢 | 中高 |
```

### 阅读时长

自动计算，不用手写。中文按 300 字/分钟、拉丁按 200 词/分钟折算，取整且至少 1 分钟。

---

## 四、写错了会怎样

frontmatter 在**构建时校验**，写错会让 `npm run build` 直接失败，并指出是哪一篇、哪个字段、为什么。常见的几种：

| 写法 | 结果 |
| --- | --- |
| 漏了 `summary` | ❌ 构建失败：Required |
| `summary` 超过 200 字 | ❌ 构建失败：超出长度限制 |
| `order: 0` 或 `order: -1` | ❌ 构建失败：必须是正整数 |
| `order: "1"` | ❌ 构建失败：是字符串不是数字（YAML 里加引号就是字符串） |
| `tags: Redis, 队列` | ❌ 构建失败：期望数组不是字符串 |
| `tags` 写成 YAML 列表（每行 `  - 值`） | ✅ 正确 |
| 字段名拼错（如 `sumary`） | ⚠️ 被静默忽略。如果拼错的是必填项会报错，但拼错**可选**项不会 —— 所以 `series` 没生效时先检查拼写 |

`tags` 是最容易写错的一个。正确写法：

```yaml
tags:
  - Redis
  - 队列
```

或者行内写法也行：

```yaml
tags: [Redis, 队列]
```

标签**大小写敏感**，`Redis` 和 `redis` 会是两个不同的标签。**不需要预先注册** —— 写了就自动出现，没人用了就自动消失。

---

## 五、一篇填好的例子

```markdown
---
title: 缓存失效这件事，我先选了最笨的办法
date: 2026-04-05
summary: 失效广播听起来更正确，但它真正难的地方在"消息丢了怎么办"。算完复杂度预算之后，我选了短 TTL 加主动删除，把省下来的精力花在了别处。
tags:
  - 缓存
  - 一致性
---

## 我一开始想做的

先说被我否掉的那个方案。写缓存失效时，直觉上最"正确"的做法是：数据变更后广播一条失效消息，所有节点收到就把本地缓存删掉。

听起来没问题。真正难的地方在于——消息丢了怎么办。

> 分布式系统里，一个"发送失败"的分支，往往比整套设计本身更花时间。

## 复杂度预算

我把两条路的维护成本列了一遍：

| 方案 | 一致性 | 要额外维护的东西 |
| --- | --- | --- |
| 失效广播 | 秒级一致 | 消息队列、重试、幂等、监控 |
| 短 TTL + 主动删除 | 最长 30 秒不一致 | 无 |

```rust
// 变更后主动删掉本节点，其余节点靠 TTL 兜底
redis.del(&key)?;
redis.expire(&key, Duration::from_secs(30))?;
```

## 结论

30 秒的不一致，业务侧能接受；那套广播的维护成本，我们当时接不住。所以我选了笨办法，把省下来的精力花在了真正的瓶颈上。

这不是说笨办法总是对的——如果业务要求秒级一致，那就必须上广播，只是那时候你得连带把重试和幂等一起做掉。
```

---

## 六、发布

```bash
npm run dev          # 本地预览 → http://localhost:4321/posts
git add src/content/posts/你的新文章.md
git commit -m "post: 文章标题"
git push             # 推上去后 GitHub Actions 会自动构建并发布
```

推完到线上生效大约需要一两分钟。部署流程和域名相关的事见 [README.md](README.md)。
